apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app: nginx
    tier: frontend
spec:
  containers:
    - name: nginx
      image: nginx
    # - name: busybox -> more than one image per pod
      # image: busybox
    # env:
      #   - name: var_name
      #     value: var_value