apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app: nginx
    tier: frontend
  annotations:
    buildversion: '1.32'
spec:
  # serviceAccountName: serviceaccountname
  # nodeSelector: # This label is in en node where you want to specifically deploy this node.
    # size: large
  restartPolicy: Never # When the pod should be restarted
  containers:
    - name: nginx
      image: nginx
      # securityContext:
        # runAsUser: 1000
        # capabilities: # only supported at container level
          # add: ["MAC_ADMIN"]

    # - name: busybox -> more than one image per pod
      # image: busybox
    # - name: log-agent
      # image: log-agent
    # env:
      #   - name: var_name
      #     value: var_value